# Future-State Navigation Map

## Primary navigation flow
Prototype home
→ Capture
→ Confirm
→ Manufacturer Evaluation
→ Recommendation Workspace
→ Present

## Static prototype navigation flow
Prototype home
→ Capture (`prototype.html`)
→ Confirm (`requirements-review.html`)
→ Optimize / recommendation review (`quote-review.html`)
→ Present (`customer-quote.html`)

## System/reference navigation
Prototype home
→ MQI System (`design-system.html`)
→ JSON tokens (`design-system.json`)

## Preserved navigation logic
- the workflow is sequential
- the evaluation step assumes candidate manufacturer comparison
- the recommendation step assumes a winning path is selected before quote output
- design system references are exposed as supporting platform artifacts

## Preservation rule
This map preserves current navigation and terminology for future rollback/reference use.